%h1 Listing monsters

%table
  %thead
    %tr
      %th= link_to "Name", monsters_url(sort: "name")
      %th= link_to "Power", monsters_url(sort: "power")
      %th= link_to "Monster type", monsters_url(sort: "type")
      %th User
      %th
      %th
      %th

  %tbody
    - @monsters.each do |monster|
      %tr
        %td= monster.name
        %td= monster.power
        %td= monster.monster_type
        %td= monster.user.name
        %td= link_to 'Show', monster
        %td= link_to 'Edit', edit_monster_path(monster)
        %td= link_to 'Destroy', monster, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Monster', new_monster_path
